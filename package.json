{
  "name": "test",
  "version": "1.0.0",
  "description": "Some scripts",
  "main": "1.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "npm run build && node dist/1.js && node dist/2.js && node dist/3.js && node dist/4.js && node dist/5.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.10.10"
  },
  "dependencies": {
    "typescript": "^5.7.3"
  }
}
